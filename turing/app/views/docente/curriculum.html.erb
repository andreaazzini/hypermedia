<% content_for :title, "Docenti • " + complete_name(params[:surname]) %>
<% content_for :aside, print_aside(DocenteController.new.aside(params[:surname])) %>

<%= create_teacher_path %>

<%= back_to_index_docente %>