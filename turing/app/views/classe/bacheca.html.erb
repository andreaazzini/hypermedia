<% content_for :title, "Classe • " + complete_class_name %>
<% content_for :aside, print_aside(ClasseController.new.aside(params[:year], params[:section])) %>

<%= create_class_path %>

<%= back_to_index_classe %>